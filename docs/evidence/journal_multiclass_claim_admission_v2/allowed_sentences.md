# Allowed Journal Extension Claims

Only `claims.json` entries with `paper_admitted=true` may be used as claims. The
exact Korean and English sentences are canonical; a manuscript may use a scoped
paraphrase only when it retains every scope and security qualification.

The paired MLP result has two distinct conclusions: the natural gap changed the
literal from S32 to S29, while the S32/S29 latency interval includes one. The
catalog S40/S29 comparison is separately admitted for the frozen 100-image
subset and one measured host.

The LeNet catalog result is
`PLAN_UNSUPPORTED_WITHIN_FROZEN_CATALOG`. It is a coverage result for seven
frozen profiles, not a proof that no CKKS catalog can support the graph.
